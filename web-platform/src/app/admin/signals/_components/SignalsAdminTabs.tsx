/**
 * DEPRECATED — SignalsAdminTabs is no longer used.
 *
 * The admin signals page now uses TierSignalSettings directly.
 * This file is kept as a stub to avoid import errors from other files
 * that might still reference it.
 */
"use client";

export function SignalsAdminTabs() {
  return null;
}
