/**
 * DEPRECATED — Manual signal creation is no longer used.
 * Signals are now generated on-demand by users via POST /api/signals/request.
 */
"use client";

export function CreateSignalForm() {
  return null;
}
