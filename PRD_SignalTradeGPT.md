# Signal Trade GPT — Product Requirements Document (PRD)

**Версия:** 1.0  
**Дата:** 28.04.2026  
**Продукт:** Telegram-бот + Веб-платформа для торговых сигналов (Pocket Option)

---

## 1. Общее видение продукта

**Signal Trade GPT** — премиальная платформа для генерации и доставки торговых сигналов для бинарных опционов (Pocket Option). Платформа состоит из двух ключевых компонентов:

1. **Telegram-бот** — основной канал доставки сигналов пользователям в реальном времени
2. **Веб-сайт** — лендинг + личный кабинет для доверия, регистрации и управления подпиской

**Цель:** Создать профессионально выглядящий продукт, который вызывает доверие с первого взгляда — как у бота, так и у сайта.

---

## 2. Целевая аудитория

- Начинающие трейдеры бинарных опционов (18-35 лет)
- Пользователи Pocket Option, ищущие сигналы
- Русскоязычная аудитория (в первую очередь), с возможностью расширения на EN

---

## 3. Фазы разработки

### Фаза 1 — MVP (2-3 недели)

> Цель: Запустить рабочий Telegram-бот с рандомными сигналами + одностраничный лендинг. Минимально жизнеспособный продукт для тестирования гипотезы.

#### 3.1 Telegram-бот (MVP)

**Функционал:**

| Функция | Описание |
|---------|----------|
| `/start` | Приветствие, краткое описание, кнопки навигации |
| Автоматические сигналы | Бот каждые N минут (настраиваемо) отправляет сигнал в канал/группу |
| Формат сигнала | Валютная пара, направление (CALL/PUT), время экспирации, уверенность AI |
| Inline-кнопки | "Открыть Pocket Option", "Мой профиль", "Статистика" |
| Статистика (фейковая) | Красивый отчёт с процентом "успешных" сигналов |
| Реферальная ссылка | Каждый пользователь получает реферальную ссылку |

**Формат сообщения-сигнала:**

```
🔴 SIGNAL TRADE GPT

📊 Пара: EUR/USD
📈 Направление: CALL ⬆️
⏱ Экспирация: 1 минута
🤖 AI Confidence: 87%
📡 Тип: AI Neural Analysis

━━━━━━━━━━━━━━━
⚡ Рекомендуемый объём: 1-3% депозита
🔗 Открыть Pocket Option →

#signal #eurusd #call
```

**Генерация сигналов (MVP — рандом):**
- Пул валютных пар: EUR/USD, GBP/USD, USD/JPY, AUD/USD, EUR/GBP, GBP/JPY, USD/CHF, NZD/USD, EUR/JPY, AUD/JPY, USD/CAD, EUR/AUD
- Направление: рандом CALL/PUT (50/50)
- Confidence: рандом 73-96%
- Экспирация: рандом из [30 сек, 1 мин, 2 мин, 5 мин]
- Интервал: каждые 5-15 минут (настраиваемый в конфиге)
- Часы работы: 08:00 - 22:00 UTC (когда рынки активны)
- Красивое оформление: эмодзи, разделители, хештеги

**Технические требования (бот):**

| Параметр | Значение |
|----------|----------|
| Язык | Python 3.11+ |
| Библиотека | aiogram 3.x (async Telegram Bot API) |
| Scheduler | APScheduler или asyncio tasks |
| Конфиг | .env файл (токен бота, ID канала, интервалы) |
| База (MVP) | SQLite (позже миграция на PostgreSQL) |
| Деплой | VPS (Ubuntu) или Docker |
| Логирование | structlog / loguru |

**Структура данных (MVP):**

```
User:
  - telegram_id (PK)
  - username
  - first_name
  - joined_at
  - referral_code (unique)
  - referred_by (nullable)
  - is_premium (bool, default false)

Signal:
  - id (auto)
  - pair (str)
  - direction (CALL/PUT)
  - expiration (str)
  - confidence (int)
  - created_at (datetime)
  - result (win/loss/pending, nullable)
```

#### 3.2 Веб-сайт (MVP — Лендинг)

**Основа:** Взять за основу приложенный HTML-файл `signal-trade-gpt-v3.html`, доработать и адаптировать.

**Секции лендинга:**

1. **Hero** — Заголовок "SIGNAL TRADE GPT", подзаголовок про AI-сигналы, CTA "Подключить бота", живой график
2. **Тикер** — бегущая строка с валютными парами и процентами
3. **Как это работает** — 4 шага (Подключи бота → Получай сигналы → Торгуй → Зарабатывай)
4. **Статистика** — Счётчики (87%+ точность, 14K+ сигналов, 4200+ пользователей, 24/7 работа)
5. **Преимущества** — AI анализ, скорость, точность, поддержка
6. **Тарифы** — Free / Premium / VIP (карточки)
7. **Отзывы** — Слайдер с "отзывами" пользователей
8. **FAQ** — Аккордеон с вопросами
9. **Footer** — Ссылки, социальные сети, дисклеймер

**Технические требования (лендинг MVP):**

| Параметр | Значение |
|----------|----------|
| Тип | Статический HTML/CSS/JS (single page) |
| Хостинг | GitHub Pages / Netlify / Vercel |
| Дизайн | Тёмная тема, красный акцент (как в приложенном файле) |
| Адаптивность | Mobile-first, все экраны |
| Анимации | Scroll-reveal, hover-эффекты, бегущий тикер |
| CTA | Кнопки ведут на Telegram-бота |

---

### Фаза 2 — Расширение (4-6 недель после MVP)

> Цель: Полноценная веб-платформа с регистрацией, личным кабинетом и админкой.

#### 3.3 Веб-платформа (полная версия)

**Стек:**

| Слой | Технология |
|------|-----------|
| Frontend | Next.js 14+ (App Router) + TypeScript |
| UI | Tailwind CSS + Shadcn/ui (вместо Ant Design — легче, современнее) |
| Анимации | Framer Motion |
| Графики | Recharts или Lightweight Charts (TradingView) |
| Backend | Next.js API Routes + Prisma ORM |
| База данных | PostgreSQL (Supabase или selfhosted) |
| Аутентификация | NextAuth.js (email + Telegram login) |
| Платежи | Криптоплатежи (Cryptomus / NOWPayments) |
| Real-time | WebSocket через Socket.io или Pusher |
| Деплой | Vercel (фронт) + Railway/VPS (бэк + БД) |

**Страницы:**

| Страница | Описание |
|----------|----------|
| `/` | Лендинг (из MVP, портированный в Next.js) |
| `/login` | Вход (email + пароль) |
| `/register` | Регистрация |
| `/dashboard` | Личный кабинет — обзор |
| `/dashboard/signals` | Список сигналов (текущие + история) |
| `/dashboard/profile` | Профиль пользователя |
| `/dashboard/referrals` | Партнёрская программа |
| `/dashboard/history` | История действий |
| `/dashboard/leaderboard` | Лидерборд |
| `/pricing` | Тарифы и оплата |
| `/how-it-works` | Как это работает |
| `/reviews` | Отзывы |
| `/faq` | FAQ |
| `/admin/*` | Админ-панель |

#### 3.4 Админ-панель

**Функционал:**

- **Пользователи:** Список, поиск, фильтры, блокировка, смена статуса, просмотр истории
- **Сигналы:** Добавление, редактирование, удаление, массовая загрузка, фильтры
- **Тарифы:** Управление тарифами, настройка цен
- **Контент:** Управление всеми текстами, FAQ, отзывами, блоками на сайте
- **Партнёрка:** Статистика, выплаты, управление условиями
- **Аналитика:** Графики роста, активности, конверсии
- **Логи:** Действия админов, история изменений

#### 3.5 Telegram-бот (расширенная версия)

| Функция | Описание |
|---------|----------|
| Премиум-сигналы | Расширенные сигналы для платных пользователей (больше пар, чаще, выше "confidence") |
| Привязка аккаунта | Связь Telegram с аккаунтом на сайте |
| Управление подпиской | Проверка/продление подписки через бота |
| Уведомления | Push о новых сигналах, истечении подписки, результатах |
| Мини-статистика | Персональная статистика за день/неделю/месяц |
| Inline-режим | Возможность шарить сигналы в другие чаты |
| Языки | Русский + English |

---

### Фаза 3 — Монетизация и масштаб (после запуска)

- Интеграция крипто-платежей
- Реферальная программа с выплатами
- Улучшенная "AI-аналитика" (можно добавить реальные индикаторы RSI, MACD и т.д.)
- A/B тестирование сигналов
- Discord-бот (параллельно Telegram)
- Мультиязычность (EN, UA, KZ)
- SEO-оптимизация сайта
- Маркетинг: YouTube, TikTok, Telegram Ads

---

## 4. Архитектура базы данных (полная версия)

```
┌──────────────────────────────────────────────┐
│                   User                        │
│  id (uuid, PK)                                │
│  email (unique)                               │
│  password_hash                                │
│  telegram_id (nullable, unique)               │
│  username                                     │
│  role (user / admin)                          │
│  status (active / banned / pending)           │
│  subscription_plan (free / premium / vip)     │
│  subscription_expires_at (nullable)           │
│  referral_code (unique)                       │
│  referred_by (FK → User, nullable)            │
│  created_at                                   │
│  last_login                                   │
│  notification_settings (jsonb)                │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│                  Signal                       │
│  id (uuid, PK)                                │
│  pair (varchar)          -- EUR/USD           │
│  direction (enum)        -- CALL / PUT        │
│  expiration (varchar)    -- 1m, 5m, etc.      │
│  confidence (int)        -- 73-96             │
│  type (enum)             -- ai / expert       │
│  entry_price (decimal, nullable)              │
│  result (enum)           -- win/loss/pending  │
│  is_premium (bool)                            │
│  created_at                                   │
│  closed_at (nullable)                         │
│  created_by (FK → User, nullable)             │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│               Subscription                    │
│  id (uuid, PK)                                │
│  user_id (FK → User)                          │
│  plan (enum)             -- premium / vip     │
│  start_date                                   │
│  end_date                                     │
│  payment_id (FK → Payment, nullable)          │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│                 Payment                       │
│  id (uuid, PK)                                │
│  user_id (FK → User)                          │
│  amount (decimal)                             │
│  currency (varchar)      -- BTC, USDT, etc.  │
│  status (enum)           -- pending/ok/fail   │
│  tx_hash (nullable)                           │
│  provider (varchar)      -- cryptomus, etc.   │
│  created_at                                   │
│  confirmed_at (nullable)                      │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│                 Referral                      │
│  id (uuid, PK)                                │
│  referrer_id (FK → User)                      │
│  referred_id (FK → User)                      │
│  created_at                                   │
│  reward_amount (decimal)                      │
│  reward_status (enum)    -- pending / paid    │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│                  Review                       │
│  id (uuid, PK)                                │
│  user_id (FK → User)                          │
│  text (text)                                  │
│  rating (int, 1-5)                            │
│  status (enum)           -- published/hidden  │
│  created_at                                   │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│                   Faq                         │
│  id (uuid, PK)                                │
│  question (text)                              │
│  answer (text)                                │
│  category (varchar)                           │
│  order (int)                                  │
│  is_active (bool)                             │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│             LeaderboardEntry                  │
│  id (uuid, PK)                                │
│  user_id (FK → User)                          │
│  period (enum)           -- day/week/month    │
│  signals_count (int)                          │
│  success_rate (decimal)                       │
│  profit (decimal)                             │
│  rank (int)                                   │
│  calculated_at                                │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│              ContentBlock                     │
│  id (uuid, PK)                                │
│  type (varchar)          -- hero, pricing...  │
│  title (varchar)                              │
│  body (jsonb)                                 │
│  order (int)                                  │
│  is_active (bool)                             │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│               AdminLog                        │
│  id (uuid, PK)                                │
│  admin_id (FK → User)                         │
│  action (varchar)                             │
│  entity_type (varchar)                        │
│  entity_id (uuid)                             │
│  details (jsonb)                              │
│  created_at                                   │
└──────────────────────────────────────────────┘
```

---

## 5. API-эндпоинты (полная версия)

### Auth
| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/auth/register` | Регистрация (email + пароль) |
| POST | `/api/auth/login` | Вход |
| POST | `/api/auth/verify-email` | Подтверждение email |
| POST | `/api/auth/forgot-password` | Восстановление пароля |
| POST | `/api/auth/telegram-link` | Привязка Telegram-аккаунта |

### User
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/users/me` | Профиль текущего пользователя |
| PUT | `/api/users/me` | Обновление профиля |
| GET | `/api/users/me/stats` | Персональная статистика |

### Signals
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/signals` | Список сигналов (с фильтрами, пагинацией) |
| GET | `/api/signals/:id` | Детали сигнала |
| POST | `/api/signals` | Создание сигнала (admin) |
| PUT | `/api/signals/:id` | Обновление сигнала (admin) |
| DELETE | `/api/signals/:id` | Удаление сигнала (admin) |

### Subscriptions & Payments
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/subscriptions` | Текущая подписка |
| POST | `/api/payments` | Создание платежа |
| POST | `/api/payments/webhook` | Webhook от крипто-платёжки |

### Referrals
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/referrals` | Статистика рефералов |
| GET | `/api/referrals/list` | Список рефералов |

### Content
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/reviews` | Отзывы |
| POST | `/api/reviews` | Оставить отзыв |
| GET | `/api/faq` | FAQ |
| GET | `/api/leaderboard` | Лидерборд |
| GET | `/api/content-blocks` | Динамические блоки контента |

### Admin
| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/admin/users` | Список пользователей |
| PUT | `/api/admin/users/:id` | Редактирование пользователя |
| GET/POST/PUT/DELETE | `/api/admin/signals` | CRUD сигналов |
| GET/POST/PUT/DELETE | `/api/admin/tariffs` | CRUD тарифов |
| GET/POST/PUT/DELETE | `/api/admin/reviews` | Модерация отзывов |
| GET/POST/PUT/DELETE | `/api/admin/faq` | CRUD FAQ |
| GET/POST/PUT/DELETE | `/api/admin/content` | CRUD контент-блоков |
| GET | `/api/admin/analytics` | Аналитика |
| GET | `/api/admin/logs` | Логи действий |

### WebSocket
| Канал | Описание |
|-------|----------|
| `ws://signals` | Real-time обновления сигналов |
| `ws://notifications` | Уведомления пользователю |

---

## 6. Дизайн и UX

### Общий стиль
- **Тема:** Тёмная (чёрный/тёмно-синий фон)
- **Акцент:** Красный (#e53030) — основной, Зелёный (#00e5a0) — для позитивных метрик
- **Шрифты:** Bebas Neue (заголовки), Manrope (основной текст), JetBrains Mono (данные/цифры)
- **Ощущение:** Премиум, технологичный, надёжный — как у профессиональной финтех-платформы

### Ключевые принципы
1. **Доверие с первого экрана** — статистика, отзывы, профессиональная подача
2. **Минимум текста, максимум визуала** — графики, иконки, анимации
3. **Mobile-first** — большинство пользователей приходят из Telegram
4. **Быстрая загрузка** — оптимизация изображений, lazy loading, SSR
5. **Профессиональные числа** — всё должно выглядеть достоверно (87.3% а не 100%)

### Дизайн бота
- Сообщения с эмодзи, но не перегружены
- Чёткая структура: пара → направление → экспирация → confidence
- Inline-кнопки для навигации
- Красивые разделители `━━━━━━━━━`
- Каждый сигнал — как "карточка" с полной информацией

---

## 7. Тарифы

| | Free | Premium | VIP |
|--|------|---------|-----|
| **Цена** | $0 | $29/мес | $79/мес |
| **Сигналов в день** | 3-5 | 15-25 | Безлимит |
| **Валютные пары** | 4 основных | Все пары | Все пары + экзотика |
| **Экспирация** | 1-5 мин | 30 сек - 15 мин | Все варианты |
| **AI Confidence** | Базовый | Расширенный | Полная аналитика |
| **Уведомления** | Канал | Канал + Личка | Канал + Личка + Push |
| **Поддержка** | — | Email | Приоритет 24/7 |
| **Лидерборд** | Просмотр | Участие | VIP-статус |
| **Партнёрка** | 10% | 15% | 20% |

---

## 8. План реализации

### Фаза 1 — MVP (сейчас → 2-3 недели)

```
Неделя 1:
├── [День 1-2] Настройка проекта, репозиторий, структура
├── [День 2-3] Telegram-бот: базовый функционал (/start, генерация сигналов)
├── [День 3-4] Telegram-бот: расписание сигналов, оформление сообщений
├── [День 4-5] Telegram-бот: реферальная система, статистика
└── [День 5-7] Тестирование бота, деплой на VPS

Неделя 2:
├── [День 1-2] Лендинг: портирование HTML, адаптация контента
├── [День 2-3] Лендинг: мобильная адаптация, оптимизация
├── [День 3-4] Лендинг: деплой, привязка домена
├── [День 4-5] Интеграция: кнопки на сайте → бот, бот → сайт
└── [День 5-7] Тестирование всего, баг-фиксы, финальный деплой
```

### Фаза 2 — Полная платформа (недели 3-8)

```
Неделя 3-4: Backend
├── Настройка Next.js + PostgreSQL + Prisma
├── Система авторизации (NextAuth.js)
├── API: Users, Signals, Subscriptions
└── Миграция бота на PostgreSQL

Неделя 5-6: Frontend
├── Портирование лендинга в Next.js
├── Личный кабинет (Dashboard, Signals, Profile)
├── Тарифы, FAQ, Отзывы
└── Адаптивный дизайн

Неделя 7-8: Админка + Интеграции
├── Админ-панель (Users, Signals, Content, Analytics)
├── Привязка Telegram-аккаунта к сайту
├── Крипто-платежи (тестовый режим)
└── Деплой полной платформы
```

### Фаза 3 — Масштабирование (после запуска)

```
├── Реальные крипто-платежи
├── Улучшенная генерация сигналов (RSI, MACD, паттерны)
├── Discord-бот
├── A/B тестирование
├── SEO + маркетинг
├── Мультиязычность
└── Мобильное приложение (опционально)
```

---

## 9. Технический стек (финальный, рекомендуемый)

| Компонент | MVP | Полная версия |
|-----------|-----|---------------|
| **Telegram-бот** | Python 3.11 + aiogram 3 | Python 3.11 + aiogram 3 |
| **Веб-сайт** | Статический HTML/CSS/JS | Next.js 14 + TypeScript |
| **UI-фреймворк** | Vanilla CSS (из HTML-файла) | Tailwind CSS + Shadcn/ui |
| **База данных** | SQLite | PostgreSQL (Supabase) |
| **ORM** | — | Prisma |
| **Авторизация** | — | NextAuth.js |
| **Платежи** | — | Cryptomus API |
| **Хостинг (фронт)** | Netlify / Vercel | Vercel |
| **Хостинг (бэк)** | VPS (Ubuntu) | Railway / VPS |
| **CI/CD** | GitHub Actions | GitHub Actions |
| **Мониторинг** | — | Sentry |

### Почему этот стек вместо предложенного в ТЗ:

1. **Next.js вместо React + NestJS** — единый проект, быстрее разрабатывать, встроенный SSR для SEO, API Routes заменяют отдельный бэкенд
2. **Tailwind + Shadcn/ui вместо Ant Design** — легче (меньше бандл), современнее, полный контроль над дизайном, лучше подходит для кастомного тёмного UI
3. **Python (aiogram) для бота вместо Node.js** — более зрелая экосистема для Telegram-ботов, удобнее для async, проще деплой
4. **SQLite для MVP** — нулевая настройка, мгновенный старт, миграция на PostgreSQL через Prisma позже

---

## 10. Структура репозитория

```
signal-trade-gpt/
├── bot/                          # Telegram-бот
│   ├── main.py                   # Точка входа
│   ├── config.py                 # Конфигурация из .env
│   ├── handlers/
│   │   ├── start.py              # /start, /help
│   │   ├── signals.py            # Команды сигналов
│   │   └── stats.py              # Статистика
│   ├── services/
│   │   ├── signal_generator.py   # Генерация рандомных сигналов
│   │   ├── scheduler.py          # Планировщик
│   │   └── formatter.py          # Форматирование сообщений
│   ├── database/
│   │   ├── models.py             # Модели SQLAlchemy / SQLite
│   │   └── db.py                 # Подключение к БД
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example
├── web/                          # Веб-сайт
│   ├── index.html                # MVP лендинг
│   ├── css/
│   ├── js/
│   └── assets/
├── docs/
│   └── PRD.md                    # Этот документ
├── docker-compose.yml
├── .github/
│   └── workflows/
│       └── deploy.yml
└── README.md
```

---

## 11. Ключевые метрики успеха

| Метрика | Цель (1 мес) | Цель (3 мес) |
|---------|-------------|--------------|
| Подписчики бота | 500+ | 3000+ |
| DAU (бот) | 100+ | 500+ |
| Конверсия Free → Premium | 3-5% | 5-8% |
| Посещения сайта | 1000+/мес | 10000+/мес |
| Рефералы | 50+ | 500+ |
| Retention (7d) | 40%+ | 50%+ |

---

## 12. Риски и митигация

| Риск | Вероятность | Митигация |
|------|------------|-----------|
| Блокировка бота Telegram | Средняя | Не спамить, соблюдать ToS, backup-бот |
| Жалобы на фейковые сигналы | Высокая | Дисклеймер "не финансовый совет", добавить реальную аналитику позже |
| Низкая конверсия | Средняя | A/B тесты, улучшение UX, маркетинг |
| Конкуренция | Высокая | Уникальный UX, качество дизайна, быстрый отклик |

---

## 13. Дисклеймеры (обязательно)

На сайте и в боте **обязательно** размещать:

> "Signal Trade GPT не является финансовым советником. Все сигналы предоставляются в информационных целях. Торговля бинарными опционами сопряжена с высоким риском потери средств. Прошлые результаты не гарантируют будущей доходности."

---

## 14. Следующий шаг

**Рекомендация:** Начать с Фазы 1 (MVP). Создать Telegram-бота с рандомными сигналами + доработать лендинг. После тестирования на реальных пользователях → переходить к Фазе 2.

Готов начать разработку MVP по этому плану. Нужно:
1. Создать Telegram-бота через @BotFather и получить токен
2. Определиться с доменным именем для сайта
3. Подтвердить/скорректировать приоритеты
